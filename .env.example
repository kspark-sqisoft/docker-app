# PostgreSQL (docker-compose / 로컬에서 동일 값 사용 권장)
DB_USERNAME=board
DB_PASSWORD=board
DB_NAME=board

# 백엔드 (로컬 nest start 시; Compose 에서는 compose 파일이 덮어씀)
# PORT=3000
# NODE_ENV=development

# 쉼표로 구분한 허용 Origin (비우면 모든 Origin 허용 — 개발 편의)
# CORS_ORIGIN=http://localhost:5173,http://localhost:8080
